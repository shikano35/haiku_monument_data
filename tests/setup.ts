// テスト環境のセットアップ
import { afterAll, afterEach, beforeAll, beforeEach } from "vitest";

// グローバルセットアップ
beforeAll(async () => {
  // テスト開始前の初期化処理
});

afterAll(async () => {
  // テスト終了後のクリーンアップ処理
});

beforeEach(async () => {
  // 各テスト実行前の処理
});

afterEach(async () => {
  // 各テスト実行後の処理
});
